# SPDX-License-Identifier: 0BSD
image: openbsd/latest
packages:
  - gmake
  - libarchive
tasks:
  - build: |
      gmake -C archivemount-ng all
      doas gmake -C archivemount-ng check
  - install: |
      doas gmake -C archivemount-ng install
