# SPDX-License-Identifier: 0BSD
image: freebsd/latest
packages:
  - gmake
  - pkgconf
  - fusefs-libs
tasks:
  - fuse: |
      sudo kldload fusefs
  - build: |
      gmake -C archivemount-ng all
      sudo gmake -C archivemount-ng check
  - install: |
      sudo gmake -C archivemount-ng install
